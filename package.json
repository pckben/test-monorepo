{
  "name": "test-monorepo",
  "private": true,
  "scripts": {
    "compile": "tsc --build --force tsconfig.json",
    "clean": "rm -rf packages/my-lib/dist packages/my-lib-examples/dist"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "typescript": "^4.5.4"
  }
}
